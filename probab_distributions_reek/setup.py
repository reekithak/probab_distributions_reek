from setuptools import setup

setup(name='probab_distributions_reek',
      version='0.1',
      description='Gaussian distributions',
      packages=['probab_distributions_reek'],
      zip_safe=False,
     download_url = "https://github.com/reekithak/probab_distributions_reek/archive/0.1.tar.gz")
